import 'package:flutter/material.dart';

class WakeOnLanView extends StatelessWidget {
  const WakeOnLanView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
