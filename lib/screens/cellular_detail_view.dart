// Flutter imports:
import 'package:flutter/material.dart';

class CellularDetailView extends StatelessWidget {
  const CellularDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
